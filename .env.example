# GitHub OAuth settings
# Create these at https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# OAuth2-Proxy cookie secret - generate with: `openssl rand -hex 16` 
# Must be exactly 16, 24, or 32 bytes (32, 48, or 64 hex characters)
COOKIE_SECRET=generate_a_random_cookie_secret_32_bytes_hex
